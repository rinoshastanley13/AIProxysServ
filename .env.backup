# PostgreSQL Database Configuration
POSTGRES_DB=ricagoapi
POSTGRES_USER=ricagoapi_user
POSTGRES_PASSWORD=changeme123
POSTGRES_PORT=5432

# Botpress Database (uses same PostgreSQL instance)
BOTPRESS_DB=botpress

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
SECRET_KEY=RICAGO

# Database Type (postgresql or sqlite)
DATABASE_TYPE=postgresql

# Botpress Configuration
BOTPRESS_PORT=3000
BOTPRESS_EXTERNAL_URL=http://localhost:3000
BP_PRODUCTION=false

# Application URLs (update for production)
# Add your production domain here
# BOTPRESS_EXTERNAL_URL=https://botpress.yourdomain.com
# API_EXTERNAL_URL=https://api.yourdomain.com
